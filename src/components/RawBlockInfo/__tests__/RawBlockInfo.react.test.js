import React from "react";
import { render } from "react-testing-library";
import RawBlockInfo from "../RawBlockInfo";
import rawBlock from "../../../../testDummies";

// test("Displays raw JSON block data", () => {
//   const div = renderer.create(<RawBlockInfo />);
//   let tree = component.toJSON();
//   expect(tree).toMatchSnapshot();
// });

// describe('RawBlockInfo', () => {
//   const props = { rawJson: rawBlock }
//   it('renders', () => {
//     const { queryByText } = render(<CommentFeed />)
//     const block_num
//   })
// })
